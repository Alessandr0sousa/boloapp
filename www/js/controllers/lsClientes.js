/*app.controller('lsCtrl', function ($rootScope, $scope, $http) {
    //$rootScope.ondeestou = "Usuário para o Google";
    
    $scope.Objeto = {};

    $http.post("http://localhost:8088/getclientes", $scope.objeto).success(confirmaCallback);

    function confirmaCallback(data, status) {
    	$scope.clientes = data;
    	console.log($scope.clientes);       
    }

});*/
